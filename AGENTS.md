# 実装エージェント向けプロジェクトガイド

このリポジトリは、Raspberry Pi 5上のROS 2と外部MCUで構成する自律走行芝刈りロボットのワークスペースです。実装、設定変更、試験、レビューを行う前に、この文書と[ドキュメントポータル](docs/README.md)を参照してください。

## 必ず守る境界

- Raspberry Pi、ROS 2、CANゲートウェイは**非安全系**です。最終的なモータ・ブレード停止は、外部MCUおよび独立したE-stop・遮断回路が担います。
- Pi、ROS 2、CANだけで安全停止を成立させる変更、またはMCU/E-stopを迂回する用途を実装してはいけません。
- CAN ID、フレーム形式、周期、timeout、状態遷移を仮定して実車へ送信してはいけません。承認済みの`docs/interfaces/can-protocol.md`が存在するまでは、実車指令を実装・有効化しません。
- 校正値はカメラ個体と取付状態に固有です。サンプル値や別個体の値を流用せず、校正記録と対応付けます。

## 作業開始時の確認事項

1. `README.md`で対象パッケージと基本的なビルド・テスト方法を確認する。
2. `docs/README.md`から、変更対象に対応する仕様、運用手順、試験基準を確認する。
3. `develop_plan.md`で現在のフェーズ、未確定事項、実機作業の前提を確認する。
4. 作業ツリーの変更を確認し、自分の作業と無関係な変更（特に`.worktrees/`）を変更・削除しない。

## 変更別の必読文書

| 変更対象 | 必ず確認する文書 |
| --- | --- |
| 全ての実装 | `README.md`、`docs/README.md`、`develop_plan.md` |
| カメラ、CameraInfo、画像設定 | `docs/camera_libcamera.md`、`docs/camera_calibration.md` |
| IMU、時刻同期、座標系 | `develop_plan.md` 第5・11・12章。将来追加される`docs/architecture/`配下 |
| CAN、MCU、速度指令、ウォッチドッグ | `develop_plan.md` 第3・4・14章。承認後は`docs/interfaces/`配下 |
| 配線、電源、GPIO、実機接続 | `develop_plan.md` 第2・11・14章。承認後は`docs/hardware/`配下 |
| 安全、停止、復帰、故障処理 | `develop_plan.md` 第1・3・9章。承認後は`docs/safety/`配下 |
| 実機試験、HIL、性能評価 | `develop_plan.md` 第8・9章。承認後は`docs/verification/`配下 |
| 依存関係、設定、校正値、リリース | `docs/configuration-management/`配下を作成後に必ず参照する |

## 実装・検証の完了条件

- パッケージ単位で変更した場合は、対象パッケージのビルドとテストを実行する。影響範囲が広い場合は、`README.md`記載のワークスペース全体の品質ゲートを実行する。
- ROSのインターフェース、パラメータ、設定ファイル、配線、CAN、状態遷移、安全上の前提を変更した場合は、対応する仕様書も同じ変更で更新する。仕様が未作成なら、実装を固定する前に仕様書を追加する。
- 実機試験では、Gitコミット、Pi OS/ROS/libcamera、MCUファームウェア、ハードウェア版、設定プロファイル、校正ファイルを試験記録に残す。
- 大容量のrosbag2データはGitへ追加しない。保存先と試験記録から参照できる識別子を残す。

## PRの記載言語

- このプロジェクトで作成・更新するPRのタイトル、本文、レビューコメントは日本語で記載する。
- コマンド、ファイル名、パッケージ名、識別子などは、必要に応じてコード表記のまま記載してよい。
- PR本文には、変更内容、検証結果、影響範囲または未検証事項を日本語で明記する。

## 文書体系の拡張

目標のディレクトリ構成、作成順、各文書の責務は[documentation-structure.md](docs/documentation-structure.md)に定義しています。未作成の仕様書を必要とする作業では、その文書を先に作成するか、少なくとも未確定事項として明示してレビューを受けてください。
