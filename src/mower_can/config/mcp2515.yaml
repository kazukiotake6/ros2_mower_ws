/**:
  ros__parameters:
    # These IDs and the byte layout are provisional. Do not enable a motor MCU
    # until docs/can_protocol.md is reviewed with its firmware implementation.
    can_interface: can0
    motion_can_id: 512
    heartbeat_can_id: 513
    command_period_ms: 20
    command_timeout_ms: 100
    max_linear_mps: 0.3
    max_angular_radps: 0.8
